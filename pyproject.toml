[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "deepwork"
version = "1.0.0"
description = "Autonomous deep research agent"
requires-python = ">=3.10"
